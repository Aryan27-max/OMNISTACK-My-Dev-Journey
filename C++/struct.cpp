#include<iostream>

struct student {
    std::string name ;
    double gpa;
    bool enrolled;
};

// struct = a structure that group related variabled under one name 
//          structs can contain mainly different data types (strings, int, double, bool, etc)

// memners o a struct can be accesed by          
// ( . ) = " class member Acess operator" 

int main (){

   


    return 0 ;
}